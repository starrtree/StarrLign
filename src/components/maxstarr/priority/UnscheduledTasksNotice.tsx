import type { RankedTask } from '@/lib/priority';
export function UnscheduledTasksNotice({ items, onOpen }: { items: RankedTask[]; onOpen: (id: string) => void }) { if (!items.length) return null; return <details className="mt-5 rounded-xl border-2 border-amber-300 bg-amber-50 p-3 text-amber-950"><summary className="cursor-pointer text-sm font-black">⚠ Needs Scheduling ({items.length})</summary><p className="mt-1 text-xs">Legacy tasks stay here until they have a real priority and schedule.</p><div className="mt-3 space-y-2">{items.map((item) => <button key={item.task.id} onClick={() => onOpen(item.task.id)} className="flex w-full justify-between rounded-lg border border-amber-200 bg-white px-3 py-2 text-left text-xs font-bold"><span>{item.task.title}</span><span>{item.reason}</span></button>)}</div></details>; }
